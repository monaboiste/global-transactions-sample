dependencies {
    testImplementation(libs.assertj)
    testImplementation(libs.easy.random)
}

test {
    useJUnitPlatform()
}

tasks.withType(Test) {
    testLogging {
        showStandardStreams = true
    }
}
