dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

rootProject.name = "global-transactions-sample"

include(":application")
include(":shared:domain")
include(":shared:event")
include(":shared:infrastructure:event-spring-bus")
include(":shared:infrastructure:persistence-h2")
include(":employee:application")
include(":employee:domain")
include(":employee:infrastructure")
