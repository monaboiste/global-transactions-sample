dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

rootProject.name = "global-transactions-sample"

include(":application")
include(":shared")

include(":event:api")
include(":event:infrastructure:event-spring-bus")
include(":event:infrastructure:persistence-h2")

include(":employee:domain")
include(":employee:infrastructure")
include(":employee:application")
