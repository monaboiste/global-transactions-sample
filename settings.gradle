dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

rootProject.name = "global-transactions-sample"

include(":shared:model")
include(":shared:infrastructure")
include(":employee:application")
include(":employee:domain")
include(":employee:infrastructure")
