info:
  application:
    name: ${project.name}
    version: ${project.version}
management:
  endpoints:
    jmx:
      exposure:
        include:
          - health
          - metrics

#spring:
#  config:
#    import: "classpath:application-employee.yml"
#  datasource:
#    url: jdbc:p6spy:h2:file:${rootDir}/data/tx;AUTO_SERVER=TRUE
#    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
#    username: sa
#    password: sa
#    hikari:
#      pool-name: shared-cp

logging:
  level:
    org.springframework.transaction: DEBUG
